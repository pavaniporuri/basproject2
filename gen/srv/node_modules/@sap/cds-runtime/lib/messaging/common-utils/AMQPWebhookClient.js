const AMQPClient = require('./AMQPClient.js')

class AMQPWebhookClient extends AMQPClient {
  constructor({ optionsMessagingREST, optionsAMQP, optionsApp, queueName, path, prefix }) {
    // set up web hooks
    super(optionsAMQP, optionsApp, prefix)
    this.optionsMessagingREST = optionsMessagingREST
    this.queueName = queueName
    this.path = path
  }

  listen(queue, cb) {}
}

module.exports = AMQPWebhookClient
